# Downloads Directory

This directory contains the Persuaid.dmg file for production downloads.

## Building and Deploying the DMG

1. **Build the DMG file:**
   ```bash
   npm run desktop:build
   ```
   This will:
   - Build the Next.js app
   - Build the Electron app and create the DMG
   - Automatically copy the DMG to `public/downloads/Persuaid.dmg`

2. **The DMG is served in two ways:**
   - Static URL: `/downloads/Persuaid.dmg` (same file on disk)
   - Download page uses `/api/download/mac`, which redirects to either:
     - `MAC_DOWNLOAD_URL` or `NEXT_PUBLIC_MAC_DOWNLOAD_URL` (full https URL, e.g. GitHub Release), or
     - `/downloads/Persuaid.dmg` if no URL env is set

3. **For Vercel deployment:**
   - Either commit `Persuaid.dmg` here (it is not gitignored by name) or set `MAC_DOWNLOAD_URL` to a hosted file URL
   - Users download from the `/download` page

## Note
The DMG file is large (~50-100MB typically). Consider:
- Using Git LFS for the DMG file
- Or building the DMG in CI/CD and uploading to a CDN
- Or hosting the DMG on a separate file hosting service
