/** Public asset — `Tutorial Video.mp4` in /public (space between words only). */
export const TUTORIAL_VIDEO_SRC = encodeURI("/Tutorial Video.mp4");

/** Welcome screen (`/welcome`) — `otherid2.mp4` in /public. */
export const WELCOME_VIDEO_SRC = encodeURI("/otherid2.mp4");
