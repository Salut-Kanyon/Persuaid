/**
 * Public URL for the Persuaid mark (`public/PersuaidLogo.png`).
 * Bump the `v` query when you replace that file so Electron’s persistent session cache
 * refreshes in `npm run desktop:dev`.
 */
export const PERSUAID_MARK_PNG = "/PersuaidLogo.png?v=20260327f";
